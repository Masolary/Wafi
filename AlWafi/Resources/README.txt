Optional bundled database location.
If alwafi_ios.sqlite is placed in this folder before generating the Xcode project, the app will load it from the bundle automatically.
Otherwise the user can import alwafi_ios.sqlite from the Files app on first launch.
